# Package lock

The standalone demo repo should commit a generated `package-lock.json` after running:

```bash
npm install
```

The template inside the main repo does not include a generated lockfile so the main project stays focused on the library source.
